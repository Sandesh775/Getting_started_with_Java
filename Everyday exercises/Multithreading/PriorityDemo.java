// Create 3 threads with priorities: MIN_PRIORITY, NORM_PRIORITY, MAX_PRIORITY
// Each thread counts to 1,000,000
// Observe which thread finishes first (usually MAX_PRIORITY)
public class PriorityDemo {
    static void main() {

    }
}